# Задача <ID> — <название>

Создано: YYYY-MM-DD · Этап: <N> (`ROADMAP.md`) · Статус: готово к выполнению
Базовый HEAD: `<hash>` · Ревью: полное | лёгкое | аналитическое
Обязательный контекст: <файлы, кроме AGENTS.md>

---

## 0. Что нужно знать до начала

Ловушки, недостающие данные, честное «что будет видно, а что нет».

## 1. Результат задачи

Одно наблюдаемое изменение: дано → действие → результат.
Чем проверяется: автотесты N/N passed | Play mode | свежая сборка.

## 2. Что реализовать

- Конфиг (какие поля JSON, какие проверки)
- Логика (чистый C#, где лежит)
- Интеграция (что показывает визуал)

### Что не трогать

## 3. Тесты

Ровно столько, сколько перечислено. Каждый — конкретное ожидаемое значение.

## 4. Критерии приёмки

1. Компиляция без ошибок и warning'ов.
2. EditMode в batch: **N/N passed**, старые тесты не сломаны.
3. Проверка тестов тестом: выпотрошить <метод> — тесты <...> краснеют.
4. Визуальная приёмка: <чем именно, или «не требуется»>.
5. Один коммит: `<type>: <message>`.

## 5. Команды проверки

## 6. Запрещено

Идея, не нужная этому шагу, пишется в `docs/DECISION_LOG.md`.
