ACCEPT

Финальная проверка Codex, 2026-09-20:

- оба обязательных дефекта из первого круга исправлены по фактическому коду;
- неизвестный id через CLI даёт `ERROR | ...`, код `2`, без traceback;
- план без `endsRun=true` даёт `completed=false` и код `3`;
- `python -m unittest tools.sim.test_sim -v`: **10/10 passed**;
- smoke CLI совпадает с задачей: контрольные времена `10,25,35,40,46,54`, код `0`;
- изменены только пять разрешённых файлов реализации; визуальная приёмка не требуется.

S1.1 принята. После отдельного коммита можно ставить S1.2 — игровой `run1.json`,
intended path и настройку темпа забега I.
